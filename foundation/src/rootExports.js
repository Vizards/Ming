/*
 * Q: 为什么要在一个 ts 项目里面写 js 文件？
 * A: 因为 ts 文件它不生效，我也没办法
 */

import { history } from 'umi';
import { generatePrivilegesTreeData } from './utils/helpers';

export { history, generatePrivilegesTreeData };
